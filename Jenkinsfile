pipeline {
    agent any
    stages {
        stage('Generic Webhook Trigger Plugin Test') {
            steps {
                echo "GitHub Pull Request Builder Plugin Test"
            }
        }
    }
}
